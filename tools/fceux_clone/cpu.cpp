// 8BitStudio FCEUX Clone - CPU (6502) Implementation
#include "cpu.h"

void CPU6502::Reset() {
	// Example: Reset registers
	// A, X, Y, PC, SP, Status
}
void CPU6502::Step() {
	// Example: Fetch opcode, decode, execute
	// uint8_t opcode = memory->Read(PC);
	// switch(opcode) { ... }
}
// Registers, opcode decoding, memory access to be implemented
